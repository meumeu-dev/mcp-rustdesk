--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,4 +1,4 @@
-mod keyboard;
+pub mod keyboard;
 /// cbindgen:ignore
 pub mod platform;
 #[cfg(not(any(target_os = "android", target_os = "ios")))]
@@ -11,7 +11,7 @@ mod server;
 mod server;
 #[cfg(not(any(target_os = "ios")))]
 pub use self::server::*;
-mod client;
+pub mod client;
 mod lan;
 #[cfg(not(any(target_os = "ios")))]
 mod rendezvous_mediator;
@@ -64,7 +64,7 @@ pub mod core_main;

 mod ui_cm_interface;
 mod ui_interface;
-mod ui_session_interface;
+pub mod ui_session_interface;

 mod hbbs_http;

